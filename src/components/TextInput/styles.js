import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    marginHorizontal: '5%',
    marginVertical: '3%',
    color: '#000000',
    fontSize: 16,
    borderRadius: 8,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 12,
    },
    shadowOpacity: 0.58,
    shadowRadius: 16.0,
    elevation: 10,
  },
});

export default styles;
